using Mapping.Data;
using Mapping.Data.Entities;
using Microsoft.EntityFrameworkCore;
using Mapping.Data.SeadData;


namespace Mapping
{
      class Program
      {
            public static async Task Main(string[] args)
            {
                  using (var context = new AppDbContext())
                  {

                  }


            }
      }
}